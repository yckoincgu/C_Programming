# Makefile
BIN = hellomake
SOURCES = hellomake.c hellofunc.c
OBJECTS = $(SOURCES:.c=.o)
CFLAGS = -g3 -Wall  


$(BIN):$(OBJECTS)
	gcc $(CFLAGS) $(OBJECTS) -o $(BIN)

%.o: %.c
	gcc $(CFLAGS) -c $< -o $@

ALL: $(BIN)

clean:
	rm -f $(BIN) $(OBJECTS)
